id = "gset"
slug = "gset"
name = "G-Set"
long_name = "Grow-only Set"
family = "set"
difficulty = "beginner"
summary = "A state-based grow-only set CRDT whose merge is set union."
kind = ["state-based", "CvRDT"]
tags = ["set", "join-semilattice", "beginner", "state-based"]

[docs]
readme = "README.md"
spec = "spec.md"
examples = "examples.md"
proofs = "proofs.md"

[rust]
crate = "crdt-algorithms"
module = "sets::gset"
type = "GSet"
status = "implemented"
source = "../../crates/crdt-algorithms/src/sets/gset.rs"
api_docs_path = "rustdoc/crdt_algorithms/sets/gset/struct.GSet.html"

[proofs]

[delivery]
causal_required = false
duplicate_tolerant = true
drop_tolerant = true
exactly_once_required = false

[features]
serde = false
no_std = false
delta = false
tombstone_free = true

[properties]
laws = [
  "join is associative",
  "join is commutative",
  "join is idempotent",
  "add is inflationary",
]
