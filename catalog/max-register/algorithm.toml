id = "max-register"
slug = "max-register"
name = "Max Register"
long_name = "Maximum Register"
family = "register"
difficulty = "beginner"
summary = "A state-based register that keeps the maximum assigned value."
kind = ["state-based", "CvRDT"]
tags = ["register", "join-semilattice", "beginner", "state-based"]

[docs]
readme = "README.md"
spec = "spec.md"
examples = "examples.md"
proofs = "proofs.md"

[rust]
crate = "crdt-algorithms"
module = "registers::max"
type = "MaxRegister"
status = "implemented"
source = "../../crates/crdt-algorithms/src/registers/max.rs"
api_docs_path = "rustdoc/crdt_algorithms/registers/max/struct.MaxRegister.html"

[proofs]

[delivery]
causal_required = false
duplicate_tolerant = true
drop_tolerant = true
exactly_once_required = false

[features]
serde = false
no_std = false
delta = false
tombstone_free = true

[properties]
laws = [
  "join is associative",
  "join is commutative",
  "join is idempotent",
  "assign is inflationary",
]
